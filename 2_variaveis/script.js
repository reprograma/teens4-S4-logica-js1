//criando variáveis

//não usamos... porém vai printar na tela
quantidadeGoiabas = 5;
//não usamos... porém vai printar na tela
var idade = 10;

let diaDeHoje = 21;

const nasciDia = 19;

console.log("quantidadeGoiabas", quantidadeGoiabas);
console.log("idade", idade);
console.log("diaDeHoje", diaDeHoje);
console.log("nasciDia",nasciDia);

// a diferença é o escopo. var surgiu justamente para dar nome às variáveis e reutilizá-las porém o escopo da var funciona globalmente em todo código o que não é útil as vezes por usarmos muitas variáveis em funções, que não queremos usar fora das funções. => let é uma boa substituta para a var. => const é uma constante, ou seja, não podemos atribuir seu valor novamente. 

//camelCase   => em JS não usamos _ ou - para nomear as variáveis, usamos mais uma letra maiuscula pra começar a próxima palavra
